msin = 
msin.datacolumn = DATA
msout = .

numthreads = 0 # the pipeline should set this number

steps = [ant, elev, freq, count]

ant.type = preflagger
ant.baseline = 

elev.type = preflagger
elev.elevation = 0 .. 20 deg

freq.type = preflagger
freq.freqrange = 10 .. 30 MHz
